export interface BusData {
    vehicleId: number;
    route: string;
    direction: string;
    latitude: number;
    longitude: number;
    destination: string;
    updatedAt: string;
    pattern: string;
}
